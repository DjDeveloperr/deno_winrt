/// Autogenerated: Windows.Win32.UI.Input.Touch._IManipulationEvents

import { GUID } from "../../../../../../guid.ts";
import { IUnknown } from "../../../../../Windows/Win32/System/Com/IUnknown.ts";

export class _IManipulationEvents extends IUnknown {
  static GUID = GUID.fromString("{4F62C8DA-9C53-4B22-93DF-927A862BBB03}");

  [Symbol.for("COMObject.name")]() {
    return "Windows.Win32.UI.Input.Touch._IManipulationEvents";
  }

  ManipulationStarted(
    x: number,
    y: number,
  ): number {
    const result = this._getFunction(3, {
      parameters: ["pointer", "f32", "f32"],
      result: "i32",
    } as const)(this._ptr, x, y);
    return result;
  }

  ManipulationDelta(
    x: number,
    y: number,
    translationDeltaX: number,
    translationDeltaY: number,
    scaleDelta: number,
    expansionDelta: number,
    rotationDelta: number,
    cumulativeTranslationX: number,
    cumulativeTranslationY: number,
    cumulativeScale: number,
    cumulativeExpansion: number,
    cumulativeRotation: number,
  ): number {
    const result = this._getFunction(4, {
      parameters: ["pointer", "f32", "f32", "f32", "f32", "f32", "f32", "f32", "f32", "f32", "f32", "f32", "f32"],
      result: "i32",
    } as const)(this._ptr, x, y, translationDeltaX, translationDeltaY, scaleDelta, expansionDelta, rotationDelta, cumulativeTranslationX, cumulativeTranslationY, cumulativeScale, cumulativeExpansion, cumulativeRotation);
    return result;
  }

  ManipulationCompleted(
    x: number,
    y: number,
    cumulativeTranslationX: number,
    cumulativeTranslationY: number,
    cumulativeScale: number,
    cumulativeExpansion: number,
    cumulativeRotation: number,
  ): number {
    const result = this._getFunction(5, {
      parameters: ["pointer", "f32", "f32", "f32", "f32", "f32", "f32", "f32"],
      result: "i32",
    } as const)(this._ptr, x, y, cumulativeTranslationX, cumulativeTranslationY, cumulativeScale, cumulativeExpansion, cumulativeRotation);
    return result;
  }
}
